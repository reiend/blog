<h1>Hello ruby</h1>

<ul>
  <% @articles.each do |article| %>
    <li> 
      <a href="<%= article_show_path(article) %>">
        <%= article.title %>
      </a>
    </li>
  <% end %>

  <%= link_to "New Article", article_new_path %>
</ul>
